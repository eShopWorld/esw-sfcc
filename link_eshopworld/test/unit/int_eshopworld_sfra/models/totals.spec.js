'use strict';

var assert = require('chai').assert;

describe('Totals', function () {
    it('cant be covered by unit test because of module.superModule usage', function () {
        assert.equal(true, true);
    });
});
